# Example API

Toy example API with core and shell (infrastructure) layout.
## How to run

1. Build
   
    ```sh
    dotnet buld
    ```
2. Run

    ```sh
    dotnet run --project Example.Api
    ```
3. View Swagger UI in browser at [http://localhost:5000/swagger](http://localhost:5000/swagger)
